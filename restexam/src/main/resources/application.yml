spring:
  application:
    name: restexam

  datasource:
    url: jdbc:mysql://localhost:3306/exampledb
    username: seolb
    password: seolb1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true
server:
  port: 80
